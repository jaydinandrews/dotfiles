# +----+
# | ls |
# +----+

alias ls='ls --color=auto'
alias l='ls -l'
alias ll='ls -lahF'
alias lls='ls -lahFtr'
alias la='ls -A'
alias lc='ls -CF'

# +-----+
# | git |
# +-----+

alias gs='git status'
alias ga='git add'
alias gp='git push'
alias gpo='git push origin'
alias gc='git commit'

# +--------+
# | neovim |
# +--------+

alias vim='nvim'

# +-------+
# | fetch |
# +-------+

alias nf='neofetch'
